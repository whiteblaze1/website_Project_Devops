apiVersion: v1
kind: Service
metadata:
  name: devops-deploy
  labels:
    app: custom
spec:
  type: NodePort 
  selector:
    app: custom
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80
    NodePort: 30008
